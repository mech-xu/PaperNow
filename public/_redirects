# PaperNow SPA fallback for Cloudflare Pages
# History mode routing: non-file paths are rewritten to index.html
# Vue Router handles the actual path on the client side
# 200 = internal rewrite (browser URL stays the same)

/* /index.html 200
